# ---- 缓存 / 运行时 / 会话 ----
backups/
cache/
debug/
file-history/
locks/
logs/
paste-cache/
plans/
projects/
runtime/
sessions/
session-env/
shell-snapshots/
tasks/
.last-cleanup
stats-cache.json
history.jsonl
proxy.log
proxy.state.json
daemon.json
daemon.port

# ---- 包含 token / OAuth / 会话状态 ----
config.json
.claude.json
.claude.json.backup
.claude.json.backup.*

# ---- 本机生成的 wrapper（每台机器路径不同） ----
bin/

# ---- 平台/IDE symlink ----
ide

# ---- plugins (machine-local state, not user-editable per official docs) ----
plugins/

# ---- 各发行版各自维护的本地配置 ----
settings.json

# ---- OS 杂项 ----
.DS_Store
Thumbs.db
desktop.ini

# ---- git 自身 ----
*.swp
*.swo
